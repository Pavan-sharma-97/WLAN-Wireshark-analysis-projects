# 📦 Data Transfer (Protected Data)

After the 4‑way handshake, data flows using:

- QoS Data frames
- A-MPDU aggregation
- Block ACKs

All frames have the **Protected** bit = 1.

---

# 💫 Key Operations
- TID-based queueing (AC_VO, AC_VI, AC_BE, AC_BK)
- EDCA contention
- Block ACK windows